
  # BinWatch IoT Platform Design

  This is a code bundle for BinWatch IoT Platform Design. The original project is available at https://www.figma.com/design/vJtr131azkUqT9fQrjs13D/BinWatch-IoT-Platform-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  